import React from 'react';
import classes from './LabelItemForm.module.css';

const LabelItemForm = () => {
  return (
    <form className={classes.form}>
        <input/>
        <button>add</button>
    </form>
  )
}

export default LabelItemForm